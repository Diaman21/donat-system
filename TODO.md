# TODO

## 🎯 ГЛАВНАЯ ЦЕЛЬ: «зелёный коридор»

Найти безопасный паттерн закупок: сколько €, как часто, в какой последовательности
телефон выдерживает до ⚠️/💀. Анализ — по `purchases` (сумма € + время + телефон +
результат). Ищем **три коридора**: 🎮 танки, 🗳 ВК и 📱 суммарный бюджет телефона
(телефон один, умирает один раз от общей нагрузки).

## ✅ Сделано

**Инфраструктура**
- [x] Схема БД в Neon (миграции `0001`–`0011`, применены вручную)
- [x] Приватный репо `Diaman21/donat-system`, CI на push: typecheck + тесты
- [x] **Тесты чистой логики** (`npm test`): разбор состава заказа и календарь МСК.
      Модули под тестом не ходят в БД — CI обходится без секретов.
- [x] **МСК-математика собрана в `format.ts`** — раньше была скопирована в
      `report.ts` и `stats.ts` (7 мест) + `cron-export.ts`.
- [x] **Отдельный бот для локальной разработки** (`TELEGRAM_BOT_TOKEN_DEV`):
      засада «не запускай `npm start`, снимет webhook» закрыта кодом, а не памятью.
- [x] **Удалена мёртвая таблица `orders`** (миграция `0011`) вместе с
      `purchases.order_id` и enum `order_status` — наследие отвергнутой «доски заказов».
- [x] Бот `@donat_system_bot` на **Vercel 24/7** (webhook, сессии в Neon)
- [x] Группа «Pattern_analyst 🧮»: команды-просмотра + авто-сводка (12:00 МСК)
- [x] **Ежедневный полный бэкап в группу** (12:05 МСК): JSON со всеми таблицами
      (восстановление один в один) + CSV покупок для Excel. История чата = архив.
- [x] **Заказы** (миграции `0008`–`0010`): список открытых + счётчики, ➕ добавить,
      🗑 отменить, пост в группу с тегами при новом заказе.
- [x] **Заказы объединены с закупками:** `✅ Выполнить` запускает цепочку закупки,
      покупки привязываются (`purchases.order_queue_id`).
- [x] **Многопозиционные заказы:** бот распознаёт состав из текста («Орден + Банки»
      = 2×€30), оператор подтверждает; заказ закрывается только когда сделаны все
      позиции. Позиции можно разводить по разным телефонам, ⚠️/💀 счётчик не двигают.
      Кнопка «✅ Продолжить заказ» прямо в итоге закупки.
- [x] **Цепочка из заказа короче:** категория и игра не спрашиваются (известны).
- [x] Уведомления об ошибках модератору, время везде по МСК

**Ввод данных**
- [x] Закупка кнопками: телефон → категория → игра → сумма → результат → заметка →
      подтверждение → интернет (📶/📡) записывает
- [x] Игры: Massive / Furious / **своя игра текстом**; €2-разогрев только в Massive
- [x] Суммы: €30 / €100 / своя текстом; для ВК — номиналы голосов
- [x] **ВК-мультизакуп** — счётчик N одинаковых покупок за раз (пишет N строк,
      при ⚠️/💀 автоматически (N−1)×✅ + 1 ошибка)
- [x] Телефоны: привязка (4 цифры IMEI + метка), ≤3 активных, 🧰 подготовленные
      (резерв вне лимита, ввод в работу, удаление), ☠️ вынужденный вывод
- [x] Предупреждение при повторе последних 4 цифр IMEI
- [x] В подтверждении покупки — счётчик «сегодня на телефоне» + «всего на телефоне»
- [x] ↩️ Откат последней записи (с воскрешением телефона, если была 💀)

**Аналитика (просмотр)**
- [x] `/stats` с периодами + разбивка по типу/играм/интернету/операторам
- [x] `/vk` — сводка голосов по дням (read-only, без советов)
- [x] `/period` — отчёт-проводник: период (пресеты + свой) → дни → телефоны дня → таймлайн
- [x] `/history` — вся история телефона, post-mortem при смерти
- [x] `/export` — CSV (модератор)
- [x] **Алерт «‼️ ВЫВОД БЮДЖЕТА»** с пингом команды: 🟠 12-й день → 🔴 13-й → 🔴🔴 просрочено
- [x] **Подсказка «когда и на сколько можно следующую»** (12.09.2026) — после записи
      танковой покупки бот считает реально списанное за скользящие 24 ч и пишет
      остаток: «можно ещё 2 тридцатки (€30 из €120) · без ограничений с завтра 08:00».
      Не блокирует, только для танков, при 💀 не показывается, при ⚠️ — про отлёжку.
      При незакрытом заказе добавляет «остальные позиции — с ДРУГОГО телефона».
      > Поставлена ПОСЛЕ покупки, а не до: бот — логгер, покупка уже совершена
      > в App Store к моменту ввода, предупреждать поздно. Зато это нужный момент,
      > чтобы назвать время следующей.
- [x] **Уточнён протокол: интервал И деньги** (12.09.2026) — гипотезу подсказал
      оператор, проверили по 197 попыткам. Можно, если интервал ≥20 ч ИЛИ сумма за
      24 ч < €120. Старое правило «никогда вторую покупку за день» данными НЕ
      подтвердилось: 3×€30 за сутки — 9 случаев, ноль смертей.
- [x] **`/corridor`** — отчёт, пересчитывающий расклад «интервал × сумма», перебор
      порогов и состояние активных телефонов прямо из базы. Чтобы границу двигали
      наблюдения, а не память.
- [x] **`/order <текст>`** (12.09.2026) — заказ одной строкой, работает и в группе.
      Кнопка и пошаговый ввод остались. В группе заведённый заказ туда повторно
      не постится; обычные сообщения заказами не становятся.
- [x] **Два блока контроля в ежедневную сводку**: «Протокол нарушен» (покупки
      в опасной клетке за сутки) и «Телефоны сейчас» (день цикла, остаток лимита,
      когда снимется ограничение). Новых cron не добавляли — лимит Hobby две.
- [x] **Итог цикла в группу** при завершении телефона: снято €, дней, слотов
      на крупные, средний интервал. История циклов копится в чате.

## 🔬 Текущий этап: работать по протоколу и копить данные

ВК на паузе (Apple заблокировали приложения ВК, конец июня 2026).
Данных на 12.09.2026: **519 покупок / €11 505 / 45 телефонов**, 14 смертей от ошибки
Apple, 17 выведены вручную, 3 в работе, 11 подготовлено в резерв.

> 🧰 Резерв из 11 трубок — подготовка к отпуску владельца: физический доступ к
> телефонам только у него, а работать будет @timka_mir один. Лимит ≤3 активных
> при этом не мешает — узкое место в количестве заказов, а не в трубках.

**🟢 Протокол танков ПОДТВЕРЖДЁН** (подробности и цифры — в `CLAUDE.md`):
разогрев €2×2 через Massive → дальше **€100/€105, одна покупка в сутки, интервал
22–28 ч, только Wi-Fi** → полные 14 дней до вывода бюджета. Никогда не делать
вторую покупку в тот же день и не тратить суточный слот на €30.

**Что подтверждено:**
- Убивает **короткий интервал**, а не сумма: <6 ч → 36% смертность, 20–36 ч → 1.4%.
- 2-я покупка за день опаснее первой в 6 раз: 4.3% (6/139) против 25.9% (7/27).
- **После фиксации протокола (13.08): 55 покупок, НОЛЬ смертей от ошибки** за месяц.
  До протокола было 7.9% — шанс случайно получить ноль ≈ 1%.
- **У коридора две границы:** длиннее 36 ч безопасно, но теряет деньги
  (7 покупок за 14 дней вместо 12). Оптимум ~24 ч.
- Деньги = число крупных покупок: 8 крупных → €839, ноль крупных (8×€30) → €244.
- Слабые модели (12 mini, 12, 14 Plus) — €36–244, под крупные суммы не годятся.

**Чего не хватает (нужны новые данные):**
- [ ] **Выжать полные 14 дней** — …8094 вывели на 10.8 дне с 8 крупными (€839).
      При ~24 ч за 14 дней влезает 11–12 крупных ≈ **€1100–1200**. Риска не требует.
- [ ] **Реальный предел спокойного телефона** — циклы подряд заканчиваются плановым
      выводом, точки смерти нет. Нужен телефон, которому дадут работать за 14-й день
      (решение бизнесовое: рискнуть остатком бюджета ради знания).
- [ ] **Мобильный интернет** — на €100 всего 5 наблюдений (2 ✅ / 3 💀) против Wi-Fi (34 ✅ / 2 💀).
      Сигнал есть, выборка мала. Нужен телефон целиком на мобильном по протоколу.
- [ ] **Сколько дадут 100% €100-слотов** — …5152 взял €794, потратив 3 слота на €30.
      Если все слоты пустить под €100 — теоретически ~€1000.
- [ ] **ВК-коридор** — гипотеза «≤800 голосов/день ок, 1000×2 дня = смерть» стоит на
      ОДНОЙ чистой смерти (…6346). Подтвердить после разблокировки ВК.
- [ ] **Белое пятно €106–119** — наблюдений НОЛЬ. Верхняя проверенная безопасная
      отметка €105, первая смерть по деньгам ровно €120. Прощупывать безопаснее
      всего тридцатками при интервале ≥20 ч. `/corridor` покажет, когда сдвинется.

## ⏸ Ждёт данных (НЕ делать раньше)

- [ ] Анализ интервалов между покупками (отчёт, а не подсказка)
      > ⚠️ Помнить: строки ВК-мультизакупа имеют ОДИНАКОВОЕ время записи —
      > серию считать одним событием, а не «N покупок в секунду».
- [ ] **Общие push-советы «сбавь темп»** — по-прежнему отложены: они меняют поведение
      оператора вообще → телефоны перестают доходить до края → смертей нет →
      коридор не уточнить. Узкая подсказка про интервал — сделана (см. выше),
      она про одно конкретное правило и этой проблемы не создаёт.
- [ ] Веб-дашборд (Next.js на Vercel) — когда данных станет много

## 🔒 Гигиена (на пользователе)

- [x] **Миграция `0012_bot_sessions_rls`** — применена 12.09.2026. Аудит нашёл, что
      на `bot_sessions` забыли включить RLS (миграция `0003`). Данные не затронуты,
      запись/чтение сессий после включения проверены.
      ⚠️ Это была НЕ дыра: бот ходит владельцем с `rolbypassrls`, политик нет ни
      у одной таблицы. Смысл появится на этапе веб-аналитики — тогда забытая
      таблица стала бы единственной открытой.

- [ ] Ротация секретов: Neon reset password + BotFather `/revoke` → обновить env на Vercel
      (токены мелькали в переписке; не срочно, но стоит сделать в спокойную минуту)
- [ ] Завести тестового бота у @BotFather и положить токен в `.env` как
      `TELEGRAM_BOT_TOKEN_DEV` — нужен, только если будешь запускать бота локально.
      Без него `npm run dev` не стартует (это защита, а не поломка).

## 🧹 Мелкая грязь в данных (не срочно)

- [ ] **Названия игр текстом:** кроме `Massive` / `Furious` в базе есть «Массив»
      кириллицей (2 покупки, €3.98) и «Soul» (1 покупка, €2) — вводились через
      «✏️ Другая игра». Разбивку по играм слегка дробят, на выводы о коридоре не
      влияют (€6 из €11 505). Слить с `Massive` при случае.
- [ ] **33 покупки без указания интернета** (€1485) — записи до миграции `0005`,
      когда поля ещё не было. Восстановить нельзя, просто помнить при анализе
      гипотезы «Wi-Fi против мобильного».

---

## ❌ Анти-цели (что мы НЕ делаем)

> Эти ограничения — осознанные. Удалять только после явного обсуждения.

- ❌ **Бот не общается с заказчиками и не ведёт доску заказов** (роль `customer`,
  карточки с `game_account`, статусы new/taken/in_progress, комментарии). Это логгер закупок.
  > ⚠️ Уточнено 09–11.09.2026: простой **список заказов** (`order_queue`) — ЕСТЬ,
  > это внутренние задачи двух операторов. Отвергнута именно доска с заказчиками.
  > С 10.09 заказы связаны с покупками через `purchases.order_queue_id` (миграция
  > `0009`) — идём ОТ заказа, лишнего шага в закупке не появилось.
  > С 12.09 наследие отвергнутой модели удалено совсем (миграция `0011`).
- ❌ **Не заводим отдельный канал для заказов.** В канале `@упоминание` не даёт
  персонального пинга, в группе даёт — группа объективно лучше и это меньше сущностей.
- ❌ **Не строим аналитику раньше данных.** Сначала данные — потом выводы и дашборды.
- ❌ **Не делаем авто-советы, пока пороги не стали законами** (см. выше — портят сбор данных).
- ❌ **Не делаем мульти-тенант / организации.** Инструмент одной команды.
- ❌ **Не зашиваем «логику разогрева» в код.** Гипотезы — в `purchase_categories.warmup_config`.
- ❌ **Не делаем кастомную авторизацию.** Идентификация по `telegram_id`, роли — вручную.
- ❌ **Не делаем платежи внутри системы.** Apple Pay — вручную, в БД только факт (€).
- ❌ **Не делаем мобильное приложение.** Telegram-бот — это и есть мобильный UX.
- ❌ **Не обходим триггер ≤3 телефонов** в коде. Правило меняем в БД — не в боте.
- ❌ **Не применяем DDL автоматически из Claude.** Все миграции схемы — руками пользователя.
- ❌ **Не удаляем покупки из истории.** Ошибочный ввод — только через ↩️ откат последней.

## 💡 Идеи на потом (не приоритет)

- Отдельное поле «модель телефона» (сейчас модель живёт в тексте метки — при 30+ телефонах
  придётся парсить лейблы; пока хватает единообразных меток вида «цвет модель»)
- Фильтр 🎮/🗳 внутри `/period`, выгрузка выбранного периода в CSV
- Миграция на платный Neon, если понадобится «не засыпающая» БД
