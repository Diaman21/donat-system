# TODO

## ✅ Сделано

- [x] Схема БД применена в **Neon** (`0001_init.sql` + `0002_add_purchase_game.sql`)
- [x] Документация: `CLAUDE.md`, `docs/db-schema.md`, `README.md`, `bot/README.md`
- [x] GitHub: приватный репо `Diaman21/donat-system`, ветка `main`
- [x] **Бот `@donat_system_bot` — MVP-логгер работает (проверено вживую):**
  - `/start` + регистрация + роли (`set-role.ts`), гейт доступа
  - привязка телефона (4 цифры IMEI + метка), ≤3 активных
  - ввод закупки: телефон → игра → сумма $ → результат (✅/⚠️/💀) → `purchases`
  - список телефонов с накопленной статистикой
  - 💀 → авто-смерть телефона (триггер)
  - Пользователи: @Boris_donat (moderator), @timka_mir (operator)

## 🎯 ГЛАВНАЯ ЦЕЛЬ: «зелёный коридор»

Найти безопасный паттерн покупок: сколько $, как часто, в какой последовательности
телефон выдерживает до ⚠️/💀. Анализ — по `purchases` (сумма $ + время + телефон + результат).
Реальная стоимость в $ — главная метрика нагрузки на телефон.

## 🎯 Следующий этап: статистика в группу

> ⚠️ Перед стартом — переключиться на **Opus**

- [ ] Создать группу (бот + ты + товарищ), добавить бота → `TELEGRAM_GROUP_ID` в `.env`
- [ ] Команда `/stats` — базовые цифры за период:
  - потрачено $ (месяц/неделя), сколько ⚠️/💀
  - сколько активных телефонов, сколько умерло
  - средний «возраст» телефона до 💀 (в покупках и в $)
  - самая длинная серия покупок (кол-во и сумма $)
- [ ] Авто-сводки в группу (раз в неделю/месяц)
- [ ] **Предупреждение о «зоне риска»**: телефон близок к историческому порогу смерти
      ($/число покупок) → бот пишет «осторожно»

## 💡 Дальше (по нарастающей)

- [ ] Подсказка следующего шага («безопасно ещё ~$30» / «пауза»)
- [ ] Post-mortem телефона при смерти (профиль: сколько прожил, суммы перед 💀)
- [ ] Анализ интервалов между покупками (убивает ли частота)
- [ ] Ручная отметка смерти телефона (без покупки)
- [ ] Поток vk_votes (категория `vk_votes`) — отдельная кнопка/раздел

## 📦 Позже: веб-аналитика (`web/`)

> ⚠️ Перед стартом — переключиться на **Opus**

- [ ] Каркас Next.js (App Router) + TypeScript на Vercel
- [ ] Подключение к Neon через Drizzle
- [ ] Дашборд: таймлайны покупок по телефонам, % done/support/long, возраст до 💀
- [ ] Экспорт CSV

---

## ❌ Анти-цели (что мы НЕ делаем)

> Эти ограничения — осознанные. Удалять только после явного обсуждения.

- ❌ **Бот НЕ ведёт доску заказов и не общается с заказчиками.** Это логгер закупок.
  Заказы/общение команда ведёт сама, вручную. (Канал, карточки, комментарии — убрали
  как «лишние переменные».)
- ❌ **Не делаем мульти-тенант / организации.** Это инструмент одной команды.
- ❌ **Не зашиваем «логику разогрева» в код.** Гипотезы — в `purchase_categories.warmup_config jsonb`.
- ❌ **Не делаем веб-аналитику раньше, чем накопятся данные.** Сначала данные — потом дашборды.
- ❌ **Не делаем кастомную авторизацию.** Идентифицируем по `telegram_id`, роли — вручную.
- ❌ **Не делаем платежи внутри системы.** Apple Pay — вручную, в БД только факт ($).
- ❌ **Не делаем мобильное приложение.** Telegram-бот — это и есть мобильный UX.
- ❌ **Не обходим триггер ≤3 телефонов** в коде. Правило меняем в БД — не в боте.
- ❌ **Не применяем DDL автоматически из Claude.** Все миграции схемы — руками пользователя.

## 💡 Идеи на потом (не приоритет)

- Миграция на платный Neon, если понадобится «не засыпающая» БД (сейчас scale-to-zero ок)
- История изменений ролей пользователей (audit log)
