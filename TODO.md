# TODO

## ✅ Сделано

- [x] Схема БД спроектирована (`supabase/migrations/0001_init.sql`)
- [x] Миграция применена в **Neon** (Postgres, EU Central, бесплатный план)
- [x] Документация: `CLAUDE.md`, `docs/db-schema.md`, `README.md`
- [x] GitHub: приватный репо `Diaman21/donat-system`, ветка `main`

## 🎯 Следующий этап: каркас Telegram-бота (`bot/`)

> ⚠️ Перед стартом — переключиться на **Opus**

- [ ] Инициализировать `bot/` (package.json, tsconfig.json)
- [ ] Установить зависимости: `grammy`, `drizzle-orm`, `postgres`, `dotenv`
- [ ] Настроить Drizzle: `bot/src/db/schema.ts` (типы из нашей схемы), `drizzle.config.ts`
- [ ] Подключение к Neon (`bot/src/db/client.ts`)
- [ ] Запуск бота `bot/src/index.ts` — `/start` регистрирует пользователя в `users`
- [ ] Хендлеры **заказчика**: создать заказ (выбор категории → игр.аккаунт → сумма)
- [ ] Хендлеры **оператора**: список заказов, взять заказ, привязать телефон (4 цифры IMEI), зафиксировать результат покупки (done / support / long)
- [ ] Хендлеры **модератора**: управление телефонами, категориями, ролями пользователей
- [ ] Обработка ошибок, логирование

## 📦 После бота: веб-аналитика (`web/`)

> ⚠️ Перед стартом — переключиться на **Opus**

- [ ] Каркас Next.js (App Router) + TypeScript
- [ ] Подключение к Neon через Drizzle
- [ ] Авторизация модераторов (через Telegram Login Widget или простой токен)
- [ ] RLS-политики для чтения данных из веба
- [ ] Дашборд: покупки во времени, % done/support/long, средний возраст телефона до 💀
- [ ] Деплой на Vercel

## 🧪 Аналитика (после набора данных)

- [ ] SQL-запросы / view для анализа разогрева: интервалы между покупками, последовательности сумм до 💀
- [ ] Сравнение `warmup_config` гипотез с фактом
- [ ] Обновление `warmup_config` без миграций через веб-интерфейс

---

## ❌ Анти-цели (что мы НЕ делаем)

> Эти ограничения — осознанные. Удалять только после явного обсуждения.

- ❌ **Не делаем мульти-тенант / организации.** Это инструмент одной команды.
- ❌ **Не открываем RLS для анонимных пользователей** на `purchases`/`orders`.
- ❌ **Не зашиваем «логику разогрева» в код бота.** Гипотезы живут в
  `purchase_categories.warmup_config jsonb` и редактируются модератором.
- ❌ **Не делаем веб-аналитику раньше бота.** Сначала данные — потом дашборды.
- ❌ **Не делаем кастомную авторизацию заказчиков.** Идентифицируем по `telegram_id`.
- ❌ **Не делаем real-time подписки** на старте. Pull-модель достаточно.
- ❌ **Не делаем платежи внутри системы.** Apple Pay — вручную, в БД только факт.
- ❌ **Не делаем мобильное приложение.** Telegram-бот — это и есть мобильный UX.
- ❌ **Не обходим триггер ≤3 телефонов** в коде. Правило меняем в БД — не в боте.
- ❌ **Не применяем DDL автоматически из Claude.** Все миграции схемы — руками.

## 💡 Идеи на потом (не приоритет)

- Уведомления модератору о смерти телефона (авто-сообщение в Telegram)
- Экспорт логов покупок в CSV
- Авто-предложение «следующего шага разогрева» оператору на основе `warmup_config`
- История изменений ролей пользователей (audit log)
- Миграция на Supabase если понадобятся Auth / Storage / Realtime
