# AGENTS.md — карта проекта donat-system

> Этот файл загружается в контекст Codex в каждой сессии.
> Здесь — суть проекта, стек, правила и текущий статус. Подробности — в `docs/`.

## 1. О проекте

**Цель.** Система управления закупкой донатов в мобильных играх. Оператор
закупает донаты через Apple Pay на iPhone. Главная задача — **найти
«зелёный коридор»**: сколько и на какие суммы (€) можно делать покупок на
телефоне, **не получив ошибку**, чтобы телефоны жили максимально долго и
давали закупать. Все покупки фиксируются в базу знаний для анализа.

**Что анализируем.** Не заказы, а **покупки**: сумма в €, частота,
последовательность сумм во времени по каждому телефону — до ⚠️/💀.
Реальная стоимость в евро — главная метрика нагрузки на телефон.
**Валюта проекта — ЕВРО (€)** везде: и танки, и ВК списываются в евро.

**Не цель.** Это не e-commerce, не магазин, не мульти-тенант SaaS. Это
внутренний инструмент маленькой команды (3 роли, ≤3 активных телефона).

## 1.1. Модель работы (РЕШЕНО — бот = логгер закупок)

Бот **не общается с заказчиками**. Заказы приходят вам в личку как раньше, вручную.
Бот делает главное: оператор в **личке** вбивает каждую закупку → бот копит данные
в БД и выдаёт аналитику.

> ⚠️ **Уточнение анти-цели (09–10.09.2026).** Раньше здесь стояло «бот НЕ ведёт заказы».
> Теперь у бота **есть список заказов** (`order_queue`, миграция `0008`) — внутренний
> список задач для двух операторов. Отказались мы от ДРУГОГО: от доски с заказчиками,
> ролью `customer`, карточками с `game_account`, статусами new/taken/in_progress и
> комментариями — это тянуло лишние переменные. Заказчики бота по-прежнему не касаются.
>
> **10.09: заказы и закупки ОБЪЕДИНЕНЫ** (миграция `0009`, `purchases.order_queue_id`).
> Раньше опасение было, что в флоу закупки полезет шаг «к какому заказу» — но идём
> НАОБОРОТ, от заказа: «✅ Выполнить» уже знает контекст, лишнего шага нет.
> На аналитику коридора не влияет — колонка nullable и в её запросах не участвует.
>
> **12.09: старая модель удалена совсем** (миграция `0011`). Пустая таблица `orders`,
> колонка `purchases.order_id` и enum `order_status` снесены — они были наследием
> отвергнутой «доски заказов», и две «заказные» колонки на `purchases` сбивали с толку.
> Рабочих таблиц теперь ровно шесть.

**Привязка телефона:** оператор вводит 4 цифры IMEI + метку → выбор «▶️ В работу»
(active) или «🧰 В подготовленные» (prepared = резерв, накачен, но не в работе).
≤3 АКТИВНЫХ (триггер БД); prepared в лимит не входят. «🧰 Подготовленные» — вкладка
резерва, кнопка «▶️ В работу» переводит prepared→active (отсчёт жизни с этого момента).

**Ввод закупки (ядро, в личке, пошагово, всё кнопками):**
1. Телефон (выбор из активных)
2. Категория (🎮 game_donate / 🗳 vk_votes) — **только в «🛒 Без заказа»**;
   из заказа пропускается (заказ по определению донат в игре).
3. Игра — только для танков: `Massive` / `Furious` / `✏️ Другая игра` (название текстом).
   Для ВК шаг пропускается (Massive/Furious — это и есть танки).
   **Из заказа тоже пропускается**, если игра известна из состава.
4. **Сумма €** — кнопки зависят от игры (см. «Типы закупок») + `✏️ Другая сумма` текстом.
   Для ВК — номиналы голосов и **счётчик мультизакупа** (🔻/🔺: N одинаковых покупок за раз).
5. Результат: ✅ done / ⚠️ support / 💀 long
6. Заметка (опц., 📝) → **подтверждение** (сводка + счётчики «сегодня на телефоне»
   и «всего на телефоне») → **выбор интернета 📶/📡 записывает покупку**
→ запись в `purchases` (operator_id = кто вбил, время — авто). При 💀 телефон → `dead` (триггер).
7. **После записи — подсказка «когда можно следующую закупку на этом телефоне»**
   (только для танков, не при 💀). Подробности — в разделе 7.
Везде есть ❌ Отмена и `/cancel`.

**Заказы — главный вход в работу (в личке):** `📥 Заказы` — открытые + счётчики
выполненных (сегодня / за неделю / всего). У каждого `✅ Выполнить`, `🗑` (отменить),
внизу `➕ Добавить заказ`. Новый заказ автоматически постится **в группу с тегами**
операторов/модераторов (в группе @упоминание реально пингует).

**`✅ Выполнить` запускает ту же цепочку закупки** (телефон → категория → игра →
сумма → результат → интернет). Заказ запоминается в `session.pendingOrderId` и
обрабатывается в самом конце, в `onNetSelected` → `closeOrderIfDone`.

**ЗАКАЗ МОЖЕТ ТРЕБОВАТЬ НЕСКОЛЬКО ЗАКУПОК** (миграция `0010`, `order_queue.items`):
«Орден + Банки» = 2×€30, «Орден + Банки + Прем год» = €30+€30+€105.
- При первом «✅ Выполнить» бот **распознаёт состав** из текста (`order-parse.ts`)
  и показывает разбор: оператор жмёт «✅ Верно» или задаёт число кнопкой 1–4.
  Состав сохраняется в `items` = `{total, list:[{label,amount,game}]}`.
- Дальше каждое «✅ Выполнить» = одна закупка. **Телефон выбирается заново каждый
  раз** — позиции одного заказа можно развести по разным телефонам.
- **Из заказа цепочка КОРОЧЕ:** шаги «категория» и «игра» пропускаются — заказ по
  определению `game_donate`, а игра одна на весь заказ и известна из состава.
  Остаётся: телефон → сумма → результат → заметка → интернет.
  Если состав задавали вручную числом (`items.list` пуст), игра неизвестна —
  её спросят, но категорию всё равно нет. В «🛒 Без заказа» спрашивается всё.
- На шаге суммы показывается подсказка `По заказу ожидается: банки — €30`.
  **Сумма НЕ подставляется автоматически** — по факту могли купить не то, что
  в заказе, а точность данных важнее экономии одного нажатия.
- Заказ закрывается, только когда успешных закупок ≥ `items.total`. Иначе:
  `📌 закупка 1 из 3 ✅, осталось: банки €30 + прем год €105` — и тут же
  **инлайн-кнопка «✅ Продолжить заказ #N»**, чтобы не гонять оператора обратно
  в список. Telegram не даёт слать инлайн-кнопку и reply-меню одним сообщением,
  поэтому при незакрытом заказе шлём кнопку (нижнее меню `persistent`, не пропадает).
- **⚠️ support / 💀 long счётчик НЕ двигают** — позиция не закрыта: саппорт
  повторяем завтра, смерть доделываем на другом телефоне. Заказ висит открытым.
- В списке заказов прогресс виден: `#7 · 1 из 3 ✅`.

> ⚠️ **Прайс позиций** (со слов оператора, 11.09.2026):
> Furious — орден €30 · банки €30 · прем год €105; Massive — ВСЁ по €100
> («вип год», «1650 золота» — одна цена). Новые позиции оператор сообщит сам.
> ⚠️ **ОДИН ЗАКАЗ = ОДНА ИГРА**, смешанных не бывает. Парсер сначала определяет
> игру (по слову «Фурио»/«Масив», иначе по первой позиции) и считает ТОЛЬКО её
> позиции — иначе слово «Золотой» в нике внутри Furious-заказа добавило бы
> фантомную Massive-закупку. Проверено на всех реальных заказах.
> ⚠️ Парсер — **подсказка, а не истина**: тексты пишут разные люди с опечатками,
> поэтому оператор всегда подтверждает кнопкой и может задать число вручную.

`pendingOrderId` сбрасывается при `/cancel` и при входе в «🛒 Без заказа» —
иначе «висящий» заказ закрылся бы посторонней покупкой.

> ⚠️ **Кнопку «Без заказа» убирать нельзя:** в базе 67 разогревочных €2 и вся ВК
> идут БЕЗ заказа. Это не рудимент, а обязательный второй вход.
> ⚠️ `orders.ts` НЕ импортирует `purchase.ts` (в `purchase.ts` уже есть импорт
> `closeOrderIfDone`) — иначе циклический импорт, опасный на Vercel-ESM.
> Поэтому `onOrderExecute` возвращает `boolean`, а `startPurchase` вызывает `bot.ts`.

**Просмотр/аналитика:** `/stats` (периоды), `/period` (отчёт-проводник: период → дни →
телефоны дня → таймлайн), `/vk` (голоса по дням), `/find` (поиск телефона по 4 цифрам IMEI),
`/recent`, `/history` (таймлайн телефона с заметками), ↩️ удалить последнюю
(с воскрешением при откате 💀), post-mortem при смерти, `/export` (CSV, модератор).

**Группа «Pattern_analyst 🧮»** (3 участника: бот, @Boris_donat, @timka_mir):
команды-просмотр + авто-сводка (12:00 МСК) + ежедневный бэкап (12:05 МСК)
+ посты о новых заказах. Бот игнорит обычные сообщения в группе; ввод — только в личке.

> Заказчики, отдельный канал, карточки заказов с полями, комментарии — НЕ делаем
> (осознанно, как «лишние переменные»). Отдельный канал для заказов тоже не заводим:
> в канале @упоминание НЕ даёт персональный пинг, в группе даёт — группа объективно лучше.
> Цель — чистые данные о покупках для «зелёного коридора».

## 2. Стек

| Слой | Технология | Назначение |
|---|---|---|
| БД | **Neon** (serverless Postgres) | Хранение данных, бесплатный план |
| ORM + миграции | **Drizzle ORM** | TypeScript-типы из схемы, миграции |
| Telegram-бот | **Node.js + TypeScript + grammY** | Логгер закупок, аналитика |
| Хостинг бота | **Vercel** (serverless, webhook) | Бот 24/7, бесплатный Hobby |
| Веб-аналитика | **Next.js + Vercel** (позже) | База знаний, дашборды |

Один язык (TS) во всём проекте — типы из Drizzle-схемы, общий tooling.

**Почему Neon, а не Supabase:** Neon бесплатен, это чистый Postgres —
наши триггеры, RLS и индексы работают без изменений. Supabase-специфичные
фичи (Auth, Storage, Realtime) нам не нужны.

## 3. Роли

| Роль | Что может |
|---|---|
| 🛍️ **customer** | роль по умолчанию при `/start` = **БЕЗ доступа**. Просьба передать ID модератору. (Заказчики в системе не работают — роль осталась в enum, но не используется.) |
| 👨‍💼 **operator** (оператор) | привязывает телефон по 4 цифрам IMEI, вбивает закупки (телефон + игра + сумма € + результат) |
| 🛡️ **moderator** (модератор) | всё + (далее) управление ролями/телефонами, веб-аналитика |

Роль назначается вручную: `bot/src/db/set-role.ts <telegram_id> <role>`.

## 4. Ключевые правила (закреплены в БД)

- **≤3 активных телефона** (триггер `enforce_max_active_phones`)
- **Уникальность IMEI только среди активных** телефонов (частичный unique index) —
  умершие телефоны с тем же IMEI остаются в истории
- **💀 `long` → телефон умирает навсегда** (триггер `handle_long_result`):
  status → `dead`, `died_at` фиксируется, `death_purchase_id` ссылается на убийцу,
  `death_reason` = `'error'` (ошибка Apple = достиг предела)
- **Причина смерти** (`phones.death_reason`): `'error'` — ошибка Apple (через 💀),
  `'forced'` — вынужденный вывод оператором (возврат бюджета). ⚠️ Для аналитики
  «зелёного коридора» `forced`-смерти НЕ считать порогом — они искусственные.
- **Разогревочные покупки без заказа** — `purchases.order_queue_id` nullable
- **RLS включён везде**; бот подключается напрямую через Postgres (DATABASE_URL)
  с полными правами; политики для веба добавим на этапе фронта

### Результат покупки — как распознать (точные диалоги Apple, интерфейс на немецком)
- ✅ `done` — покупка прошла.
- ⚠️ `support` — **временный отказ платежа, телефон ЖИВ.** Диалог:
  > «**Dein Kauf konnte nicht abgeschlossen werden.** Für Unterstützung wende dich an
  > den iTunes Support unter apple.com/support/itunes/ww/» (кнопка `OK`).

  «Покупку не удалось завершить, обратись в iTunes Support». Отлежка ~сутки → можно
  повторить. (Отсюда и название категории `support`.)
- 💀 `long` — **телефон мёртв НАВСЕГДА.** Признак смерти — всплытие waiver-диалога:
  > «**Bestätigung.** Ich bestätige, dass ich diesen Kauf nicht mehr stornieren kann,
  > wenn ich diesen In-App-Kauf innerhalb von **vierzehn Tagen**, nachdem ich auf
  > „Kaufen" getippt habe, lade oder verwende.» (кнопки `Abbrechen` / `Kaufen`).

  «Подтверждаю, что не смогу отменить покупку в течение 14 дней…». ⚠️ **Убивает САМО
  ПОЯВЛЕНИЕ окна, а не нажатие «Kaufen».** Как увидел — телефон списан, больше не
  работаем (жать ничего не нужно). Это не сбой покупки, а **флаг аккаунта/устройства**
  (европейский отказ от права возврата за 14 дней — Apple показывает при подозрении).

> ⚠️ **Тонкость для аналитики.** Строка 💀 `long` — это ПОПЫТКА, на которой всплыл
> waiver, а НЕ завершённая оплата: её `amount` (€30/€100) телефон по факту **не
> потратил** (окно всплыло вместо списания). При подсчёте «сколько € телефон вытянул»
> сумму убившей строки считать как не-потраченную. На частоту/паттерн смертей это не
> влияет, но «€ до смерти» слегка завышен на номинал последней покупки.

### Гигиена данных (правила ввода)
- **Вбивать покупку сразу после совершения**, не пачкой вечером: `purchased_at` = время
  записи, на нём держится вся аналитика темпа/интервалов.
- **Мультизакуп с ⚠️/💀 разбивается автоматически:** ошибка всегда у последней попытки,
  бот пишет (N−1)×✅ + 1×⚠️/💀 (иначе N строк long исказили бы аналитику смертей).
- ⚠️ **Ограничение:** строки серии мультизакупа получают ОДИНАКОВОЕ время записи.
  Будущий анализ интервалов должен считать серию одним событием, а не «N покупок в секунду».
- Вынужденное отключение телефона (бюджет/блокировка) — только через «☠️ Вывести»
  (`forced`), НЕ через 💀: иначе искусственная смерть попадёт в коридор как настоящая.

### Типы закупок (расширяемо, в `purchase_categories`)
- `game_donate` — **суммы зависят от игры** (цены в играх разные, задано в коде
  `GAME_AMOUNTS` в `purchase.ts`), везде плюс «✏️ Другая сумма» текстом:
  - **Massive:** €2 и €100. **€30 в Massive нет.** €2 — разогрев, ведём только здесь.
  - **Furious:** €30 и **€105** (большая покупка в Furious всегда стоила €105).
  - Другая игра: общие суммы из `denominations` категории.
- `vk_votes`: номиналы голосов — 10 гол/€0.99, 18/€1.99, 28/€2.99, 40/€3.99.

> ⚠️ **Furious €105 — только ОДИН раз на телефон.** Второй такой покупки в игре не бывает.
> Бот подсвечивает это в «☎️ Телефоны» (❗️ у телефона) и в подтверждении покупки.
> Исторические записи Furious €100 исправлены на €105 (26.08.2026, 10 строк) —
> цена всегда была €105, записывали неточно.

### ⚠️ Принцип анализа: ТРИ коридора (телефон общий, но методики разные)
Танки и ВК делаются на ОДНИХ телефонах, и **методики прогрева у них совершенно
разные** (см. `warmup_config`). Поэтому ищем **три «зелёных коридора», а не один**:

1. **🎮 Танки** — свой коридор: сколько €2→€30→€100 и в каком темпе телефон держит.
2. **🗳 ВК** — свой коридор: сколько серий по 40 голосов (€3.99) и как часто держит.
3. **📱 Суммарный бюджет телефона** — потому что телефон ОДИН и жизнь у него ОДНА:
   он умирает один раз от **суммарной** нагрузки. ВК и танки едят один ресурс —
   тяжёлый день ВК уменьшает запас под танки, и наоборот. Игнорировать нельзя.

Поэтому считаем И раздельно (коридоры 1, 2), И вместе (коридор 3). Разбивка
🎮 Танки / 🗳 ВК в `/stats`, истории и post-mortem — для коридоров 1 и 2.

> ⚠️ **«Грязные» точки смерти.** Если телефон умер на ВК-покупке, но до этого был
> нагружен танками (или наоборот) — это НЕ чистая точка коридора ВК. Для чистого
> разделения коридоров полезно вести часть телефонов преимущественно под ВК,
> часть — под танки. Смешанные смерти годятся только для коридора 3 (суммарного).

### ⚠️ Правило разогрева (важно для аналитики)
- **`game_donate` €2 — ВСЕГДА разогрев телефона**, не заказ (заказов на €2 не бывает).
- **`game_donate` €30 / €100 — реальные закупки** (боевая нагрузка по заказам).
- **`vk_votes` — всегда заказы** (включая мелкие покупки), это НЕ разогрев.
- Различается аналитикой по `category` + `amount` (отдельное поле не нужно). При поиске
  «зелёного коридора» разогрев (€2) и боевые закупки учитываются с этим контекстом.

### 🟢 РАБОЧИЙ ПРОТОКОЛ ТАНКОВ
> Зафиксирован 13.08.2026 (179 покупок / 13 смертей) → **ПОДТВЕРЖДЁН 09.09.2026**.
> После фиксации: **55 танковых покупок, НОЛЬ смертей от ошибки Apple** за ~месяц.
> Счётчик error-смертей стоит на 14 с 13.08. Все 8 завершённых телефонов дожили
> до планового вывода бюджета живыми. До протокола было 7.9% смертности —
> вероятность получить ноль смертей на 55 покупках случайно ≈ 1%. Правило работает.

**Главное открытие: убивает не сумма и не «покупок в день», а КОРОТКИЙ ИНТЕРВАЛ
между соседними покупками на одном телефоне.**

Смертность танковых покупок по интервалу с предыдущей:

| Интервал | Покупок | 💀 | Смертность |
|---|---|---|---|
| < 6 ч | 11 | 4 | **36%** |
| 6–12 ч | 17 | 3 | 18% |
| 12–20 ч | 16 | 2 | 13% |
| **20–36 ч** | **69** | **1** | **1.4%** ✅ |
| > 36 ч | 30 | 3 | 10% * |

\* все три смерти при >36ч имеют внешнюю причину (…0166 — танк после недели тяжёлого ВК,
…5700 — целиком на мобильном, …0524 — проба в новой игре). Длинная пауза сама по себе не вредит.

То же в другом разрезе: **1-я покупка за день — 4.3% смертность (6/139),
2-я покупка за день — 25.9% (7/27)**, в 6 раз выше. 7 из 13 танковых смертей — на второй покупке за день.

**ПРОТОКОЛ (применять по умолчанию):**
1. **Дни 1–2:** разогрев €2 через Massive, по ОДНОЙ покупке в день.
2. **Дни 3–14:** **€100/€105, ровно одна покупка в сутки, интервал 22–28 часов.**
3. **Всегда Wi-Fi** (на мобильном €100 дал 3 смерти из 5).
4. **Отработать ПОЛНЫЕ 14 дней**, не выводить раньше срока (бот предупреждает с 12-го).
5. **НИКОГДА не делать вторую покупку в тот же день** — даже «маленькую» €2 или €30.

**⚠️ У коридора ДВЕ границы, а не одна (уточнение 09.09.2026):**
- Короче 20 ч — **смерти** (€36–246): …3685 (11 ч), …6408 (16 ч), …1464 (15 ч).
- 20–30 ч — **оптимум** (€409–839).
- Длиннее 36 ч — **безопасно, но убыточно**: за 14 дней влезает 7 покупок вместо 12.
  …5426 (60.8 ч) дал €341, …8520 (44.2 ч) — €439 против €839 у …8094 (25.1 ч)
  за те же 14 дней. Растягивать паузы — терять деньги, а не покупать безопасность.

**Почему €100, а не €30:** слот в сутках один, риск слота одинаковый, а €100 приносит
втрое больше. Связь почти прямая — сколько слотов потрачено на крупные, столько и денег:
…8094 8 крупных → €839 · …5152 7 → €799 · …6538 5 → €559 · …5801 ноль (8×€30) → €244.
…1023 потратил 11 слотов на €30 и взял €634 — сотнями вышло бы под €1400.

**Результаты протокола:** …8094 — **€839** за 11 транзакций (рекорд, выведен живым),
…5152 — €799, …1023 — €634, …6538 — €559. Нарушители интервала: …6408 (€246) и
…3685 (€242) били по 2 раза в день и умерли за 6 дней — **втрое хуже**.

**Где остался запас:** …8094 вывели на 10.8 дне с 8 крупными покупками. При интервале
~24 ч за полные 14 дней влезает **11–12 крупных ≈ €1100–1200**. Это ближайшая цель,
и она не требует риска — только не выводить раньше срока и не тратить слоты на €30.

> ⚠️ Ограничения: в безопасной корзине 20–36 ч есть 1 смерть из 87 (1.1%) — это не ноль.
> …5801 «12 mini» шёл с интервалом 17 ч и всё же дожил до вывода (но дал лишь €244) —
> одно исключение картину не ломает. Правило Wi-Fi — 5 наблюдений на мобильном,
> держим как дешёвую страховку, не как доказанный закон.
> ⚠️ **Побочный эффект успеха:** ноль смертей отчасти потому, что опасное перестали делать.
> Границу больше не прощупываем — истинный предел телефона так и не измерен, четыре цикла
> подряд закончились плановым выводом, а не смертью.
> **Всё это про танки.** ВК жил по другой логике (серии по 5 покупок подряд, телефоны доходили
> до €416–441) — после разблокировки ВК проверять заново.

### 🔥 Методика прогрева (стартовые гипотезы в `warmup_config`)
Записаны раздельно по категориям. Правка: `bot/src/db/set-warmup.ts` (методики в коде, перезапустить).
- **game_donate (Massive/Furious):** 2–3 дня по €2 (разогрев) → затем боевые суммы.
  **Разогрев (€2) — только через Massive** (кнопка €2 в боте есть только там).
  Боевые: Massive — €100, Furious — €30 и €105 (последняя однократно на телефон).
- **vk_votes:** ВК ~2 недели/мес. Номиналы: 10 гол/€0.99, 18/€1.99, 28/€2.99, 40/€3.99.
  День 1: серия 4 покупки по 40 голосов (€3.99), повторить 2–3 раза с интервалом
  в несколько часов; далее лимит растёт. **Методика ВК отличается от игр.**
- Цель — выявить/уточнить точный цикл и коридор по данным (анти-цель: не вычислять раньше данных).

## 5. Архитектурные решения

- **Покупки — ядро базы знаний.** Каждая покупка — точное время + телефон +
  сумма € + результат + игра + интернет. Этот лог анализируем для «зелёного коридора».
  Если закупку делали по заказу — заполняется `purchases.order_queue_id` (nullable;
  у разогрева €2 и ВК он пуст, таких большинство).
- **Циклическая ссылка** `phones.death_purchase_id → purchases.id` — храним
  «какой именно покупкой убит телефон» для post-mortem анализа.
- **Категории расширяемы.** `purchase_categories.warmup_config jsonb` — здесь
  гипотезы разогрева, можно менять без миграций. Закупка по умолчанию = `game_donate`.
- **Drizzle только для типобезопасных запросов**, НЕ для миграций. Схема в
  `bot/src/db/schema.ts` — зеркало применённого SQL. Миграции схемы — руками
  (см. раздел 8). `bot/src/db/*.ts` содержит утилиты: `check`, `set-role`, `users-list`.
  > ⚠️ Важное следствие для миграций: **нигде нет `db.select().from(purchases)` без
  > проекции** — все запросы к покупкам перечисляют колонки явно, а бэкап читает
  > сырым `select *`. Поэтому удаление колонки не ломает «старую» сборку на Vercel
  > в момент деплоя. Если когда-нибудь появится `select()` без проекции — это
  > перестанет быть верным, проверять перед следующей такой миграцией.
- **Даты и время — только через `bot/src/format.ts`.** Там же вся арифметика МСК
  (`mskTodayIso`, `daysBetweenIso`, `addDaysIso`, `ddmmOf`, `hhmmMsk`). Раньше она
  была скопирована в `report.ts` и `stats.ts` — правка в одном месте развела бы
  отчёт со статистикой. Копии в новых файлах не заводить.
- **Тесты — только для чистой логики.** `npm test` (Node test runner + tsx) гоняет
  `order-parse`, `format` и `interval`: разбор состава заказа, календарь МСК и окно
  следующей закупки. Модули под тестом намеренно не ходят в БД, поэтому CI обходится
  без секретов. Интерфейс бота тестами не покрываем — дорого и малополезно.
  > Правило, которое себя оправдало: если логику стоит покрыть тестом — вынеси её
  > в модуль без импорта `db/client.js`. Так появились `order-parse.ts` и `interval.ts`.

## 6. Структура репозитория

```
donat-system/
├── CLAUDE.md               ← карта проекта (загружается в Claude каждую сессию)
├── AGENTS.md               ← та же карта для Codex, ПЕРЕСОБИРАЕТСЯ из CLAUDE.md sed'ом
├── README.md               ← краткое описание для людей
├── TODO.md                 ← задачи и анти-цели
├── .env / .env.example     ← ключи (.env не в git)
├── docs/db-schema.md       ← схема БД + бэкап/восстановление
├── supabase/migrations/    ← 0001…0011, применяются ВРУЧНУЮ в Neon
├── .github/workflows/      ← CI: typecheck + npm test на каждый push
└── bot/
    ├── api/                webhook.ts · cron-report.ts · cron-export.ts
    ├── vercel.json         builds + routes + 2 crons (явно)
    └── src/
        ├── bot.ts          роутинг: команды, hears (по слову), callback, текст по flow
        ├── index.ts        локальный запуск — ТОЛЬКО с TELEGRAM_BOT_TOKEN_DEV
        ├── context.ts      FlowState — состояния пошагового ввода
        ├── commands.ts     список команд для меню Telegram
        ├── format.ts       время и календарь МСК (единственное место)
        ├── format.test.ts  тесты календаря
        ├── db/             client · schema (зеркало SQL) · session-store · утилиты
        └── handlers/       purchase · phones · orders · order-parse (+ .test) ·
                            interval (+ .test) · stats · vk · report · history ·
                            recent · postmortem · export · backup · menus
```

> ⚠️ **AGENTS.md — не править руками.** Он генерируется из `CLAUDE.md` командой sed
> (замена заголовка и упоминаний Claude→Codex/агент). После правки `CLAUDE.md`
> пересобирать — иначе разъедутся.

Будущие папки (когда дойдёт черёд):
- `web/` — Next.js приложение (веб-аналитика)

## 7. Текущий статус (СИСТЕМА ГОТОВА К РАБОТЕ)

- ✅ Схема БД в Neon: миграции **`0001`…`0011`** применены
  (`0001_init` · `0002_purchase_game` · `0003_bot_sessions` · `0004_phone_death_reason` ·
  `0005_purchase_internet` · `0006_vk_units` · `0007_phone_prepared` · `0008_order_queue` ·
  `0009_purchase_order_link` · `0010_order_items` · `0011_drop_legacy_orders`).
  Рабочих таблиц шесть: `users` · `purchase_categories` · `phones` · `purchases` ·
  `order_queue` · `bot_sessions`.
- ✅ БД: Neon (serverless Postgres, scale-to-zero), EU Central, бесплатный план
  > ⚠️ **Панель Neon (console.neon.tech) у пользователя заблокирована** (экран
  > «Access Blocked», с 09.09.2026) — предположительно блокировка по IP/региону.
  > **Сама БД работает нормально:** бот ходит напрямую по `DATABASE_URL`, панель ему
  > не нужна. Следствие: пользователь не может запускать SQL сам, см. раздел 8.
- ✅ GitHub: приватный репо `Diaman21/donat-system`, ветка `main`.
  CI (`checks`): `npm run typecheck` + `npm test` на каждый push, Node 24.
- ✅ **Бот `@donat_system_bot` на Vercel 24/7 (webhook).** Команды: start, **order,
  orders**, stats, vk, period, phones, find, recent, history, report, export, help, cancel.
  **📥 Заказы** — главный вход: ✅ Выполнить запускает закупку и закрывает заказ при
  успехе, 🗑 отменить, ➕ добавить; новый заказ постится в группу с тегами.
  **🛒 Без заказа** — закупка, не связанная с заказом (разогрев €2, ВК).
  **🔍 Поиск по IMEI** — 4 цифры → была ли трубка в работе, метка, статус, итоги
  (показывает ВСЕ совпадения: цифры повторяются, умершие остаются в истории).
  Закупки (категория/игра/
  сумма-кнопками/результат/заметка/подтверждение; **ВК-мультизакуп** — счётчик кол-ва
  одинаковых покупок за раз), привязка/вывод телефонов, /stats с периодами,
  **/vk (read-only сводка ВК по дням: голосов/день, ⚠️/💀 по телефонам)**,
  /history (таймлайн+заметки), /recent, удаление последней, post-mortem,
  /export CSV, уведомления об ошибках модератору, время по МСК.
  Кнопки меню матчатся по слову (не по эмодзи) — иконки можно менять свободно.
- ✅ **Группа «Pattern_analyst 🧮»:** команды-просмотр + авто-сводка (Vercel Cron, 12:00 МСК)
  + **ЕЖЕДНЕВНЫЙ полный бэкап в группу** (Vercel Cron, 12:05 МСК; история чата = архив):
  `backup-full-*.json` — сырые строки всех рабочих таблиц (восстановление один в один)
  + `purchases-*.csv` для Excel. ⚠️ Только CSV недостаточно: там нет меток телефонов,
  `death_reason` и `phone_id` — анализ коридора из него не восстановить. Как восстанавливать —
  в `docs/db-schema.md`.
  ⚠️ Лимит Vercel Hobby — 2 cron-задачи: обе заняты (сводка + бэкап).
  **📥 Новый заказ → пост в группу** с тегами операторов/модераторов (в группе
  @упоминание реально пингует, в отличие от канала). Ошибка отправки не ломает
  сохранение заказа. Отдельный канал сознательно НЕ заводим — лишняя сущность.
  В сводке/`/stats` — громкий блок «‼️ ВЫВОД БЮДЖЕТА» с пингом операторов/модераторов:
  бюджет выводится на **14-й день после первой покупки** на телефон, предупреждаем с 12-го
  (🟠 12 → 🔴 13 «завтра» → 🔴🔴 14+ «просрочено»). Блок появляется только при наличии таких телефонов.
- ✅ Пользователи: @Boris_donat (moderator), @timka_mir (operator).
- ✅ **Хостинг: Vercel (Hobby, webhook), сессии в Neon.** Деплой из `bot/` (Root Directory = bot).
- ⏳ **Текущий этап: копить данные по ТАНКАМ.** ВК на паузе — Apple заблокировали
  приложения ВК (конец июня 2026), сбор ВК-данных встал до возврата приложений.
  > 📊 **Накоплено (на 12.09.2026):** 519 покупок / €11 505.08 / 45 телефонов
  > (14 умерли от ошибки Apple, 17 выведены вручную, 3 в работе, **11 подготовлено**).
  > Результаты: ✅ 500 · ⚠️ 4 · 💀 15. Заказы: 6 закрыто, 0 открытых.
  > По типу: 🎮 танки €10 351 (229 покупок) · 🗳 ВК €1154 (290 покупок, 11 570 голосов).
  >
  > 🧰 **11 подготовленных — это НЕ простой, а запас перед отпуском владельца:**
  > физический доступ к телефонам есть только у него, а работать будет @timka_mir
  > один. Лимит ≤3 активных при этом не мешает: узкое место — количество заказов,
  > а не количество трубок.
  >
  > ✅ **Связка «заказ → закупка» ПРОВЕРЕНА в бою** (11.09 вечером): заказ #5
  > («Орден +Прем год») распознан как 2 позиции, обе покупки привязались,
  > заказ закрылся автоматически только после второй. Привязанных покупок: 4.
  > У заказов #1–#4 `order_queue_id` пустой — они закрыты ДО деплоя связки,
  > по старой логике. Это нормально, не баг.
  > Пропуски в нумерации заказов (#6) — след теста в откаченной транзакции:
  > `bigserial` не откатывается. На данные не влияет.
  >
  > ⚠️ **Мусор в названиях игр** (мелочь, но искажает разбивку): кроме `Massive`
  > и `Furious` в данных есть «Массив» кириллицей (2 покупки, €3.98) и «Soul»
  > (1 покупка, €2) — вводились текстом через «✏️ Другая игра». На выводы о коридоре
  > не влияют (€6 из €11 505), но при чистке данных их стоит слить с `Massive`.
  > Ещё у 33 покупок (€1485) не указан интернет — это записи до миграции `0005`.
  >
  > ⚠️ **Время трёх покупок 11.09 правил вручную** (16:21 / 16:21 / 17:16 МСК):
  > оператор сделал их до настройки логики и вбил задним числом. Сверял
  > контрольные суммы до/после — изменилось только время. Если снова понадобится,
  > порядок такой: оператор вбивает через бота → называет реальное время →
  > правлю `purchased_at` (и `died_at`, если была смерть) со сверкой.
  >
  > 🎮 **ГЛАВНОЕ — ИНТЕРВАЛ между покупками**, см. «Рабочий протокол танков» выше —
  > он ПОДТВЕРЖДЁН на новых данных (0 смертей за месяц работы по протоколу).
  > Ранняя формулировка «темп ≤1.2 покупки/день» была тенью этой причины: низкий
  > дневной темп автоматически даёт длинные интервалы. Мерить надо ИНТЕРВАЛ.
  >
  > ⏳ **Ограничитель сменился:** телефоны по протоколу доживают до планового вывода
  > бюджета, НЕ умирая (…8094 €839/10.8дн, …5152 €799/12.2дн, …1023 €634/13.8дн,
  > …5823 €494/14.1дн, …8520 €439/14.2дн). Вопрос сместился с «когда умрёт»
  > на **«сколько снять за 14 дней»**. Рекорд — **€839**. Потолок протокола ≈ €1100–1200,
  > если отработать полные 14 дней и не тратить слоты на €30.
  >
  > 📱 **Модель телефона важна:** слабые (12 mini, 12, 14 Plus) дают €36–106 и под €100
  > не годятся; Pro/новые — €400–650. Модель живёт в тексте метки («Чёрный 16 про»).
  >
  > 🌐 **Мобильный интернет под подозрением:** €100 через мобильный — 2 ✅ / 3 💀,
  > через Wi-Fi — 21 ✅ / 2 💀. Сигнал есть, но 5 наблюдений мало и там сидит …5700
  > целиком на мобильном. Держим правило «€100 с Wi-Fi» как дешёвую страховку, не как закон.
  >
  > ❌ **Опровергнутые гипотезы** (держать в уме, чтобы не воскресить):
  > «телефон вырабатывает ~€245» — нет; «денежный потолок €400–550» — нет (…5152 дал €794
  > живым); «убивает сумма покупки» — нет, убивает короткий интервал; «темп покупок в день» —
  > работало как грубое приближение, точная величина = интервал между соседними покупками;
  > «чем реже покупки, тем лучше» — нет, длиннее 36 ч безопасно, но теряет деньги (оптимум ~24 ч).
  > 🗳 **ВК-гипотеза (зафиксирована до паузы):** ~до 800 голосов/день безопасно;
  > **1000/день — край, два дня по 1000 подряд = смерть** (чистая смерть …6346:
  > разгон 400→600→800→1000→1000💀, 106 покупок/€420). 8846 после 1000 откатился
  > к 800 и выжил. Чистая ВК-смерть пока ОДНА — подтвердить как закон при +2–3 смертях
  > после разблокировки. 0166 давал ⚠️ на 440–680/день без смерти (мягкий потолок).
- ✅ **СДЕЛАНО 12.09.2026: подсказка «когда можно следующую закупку»** (`interval.ts`).
  После записи танковой покупки бот пишет окно следующей:
  ```
  ⏳ Следующая закупка на этом телефоне:
     🟢 лучше всего: завтра 21:47 — послезавтра 03:47
     ⛔️ не раньше завтра 19:47 (раньше — зона риска)
  ```
  Числа из протокола: «не раньше» = 20 ч (нижняя граница коридора), оптимум = 22–28 ч.
  > **Почему ПОСЛЕ покупки, а не до** (решение владельца, 12.09): бот — логгер,
  > покупка совершается в App Store и только потом вбивается сюда. Предупреждать
  > в момент ввода бессмысленно — деньги уже списаны. Зато это точный момент,
  > чтобы сказать, когда можно следующую: оператор как раз здесь и смотрит.
  >
  > **Только для танков** (`game_donate`). У ВК методика противоположная — серии
  > покупок подряд, — там порог 20 ч был бы неверен и превратился бы в спам.
  > **При 💀 не показывается вовсе:** телефон мёртв, следующей покупки не будет.
  >
  > **При незакрытом заказе добавляется строка** «остальные позиции — с ДРУГОГО
  > телефона». Именно здесь протокол и нарушался: заказ «Орден + Банки» = 2×€30,
  > и обе позиции уходили на один телефон в один вечер (8 таких дней за 30 дней
  > на момент внедрения, смертей не было, но паттерн исторически давал 25.9%).
  >
  > **Почему это НЕ нарушает анти-цель «не делаем авто-советы».** Отложены были
  > советы вида «сбавь темп»: они меняют поведение вообще → телефоны перестают
  > доходить до края → смертей нет → коридор не уточнить. Здесь подсказка узкая:
  > одно правило, один телефон, конкретное время. К тому же край мы и так больше
  > не щупаем (упираемся в количество заказов), так что терять нечего.
- ❌ Ждёт данных (НЕ делать раньше): общие push-советы «сбавь темп», подсказки шага,
  веб-дашборд. См. `TODO.md`. Для просмотра — **pull-отчёты** `/vk`, `/period`, `/stats`.
  >
  > ⚠️ **Смерти бывают и на €2-разогреве** (…0524, …7057) — старое утверждение
  > «на €2 никогда» ОШИБОЧНО. Оба случая с внешней причиной (проба в новой игре /
  > телефон без отлёжки после прошлой работы), но правило «€2 безопасен» не действует.

> ⚠️ Нюансы Vercel (важно при правках): импорты с `.js` (ESM), адаптер `webhookCallback`
> = `'https'`, функции в `vercel.json` явно, сессии в БД. Деталей — `bot/README.md`.
>
> ✅ **Засада «не запускать `npm start` локально» ЗАКРЫТА КОДОМ** (12.09.2026).
> Раньше это было правилом в документации, то есть держалось на памяти: забыл —
> `index.ts` делал `deleteWebhook` боевому боту, и прод замолкал молча. Теперь
> локальный запуск работает только с отдельным ботом (`TELEGRAM_BOT_TOKEN_DEV`),
> без него останавливается и объясняет, что делать. Заводится у @BotFather за минуту.
> ⚠️ База у тестового бота **общая с боевой** — покупки, вбитые через него, попадут
> в реальную статистику. Для проверки интерфейса нормально, для экспериментов — нет.

## 8. Рабочий процесс (важно)

1. **План → ждать «ок» → код.** Никаких изменений кода без явного подтверждения.
2. **Переключай на Opus** перед критичными задачами: миграции, каркасы,
   RLS, деплой. агент напоминает об этом сам.
3. **SQL руками для схемы.** Структурные изменения БД — новый файл
   `supabase/migrations/000N_*.sql`, который пользователь запускает в Neon SQL Editor.
   агент НЕ применяет DDL автоматически. После — обновить `bot/src/db/schema.ts`.
   > ⚠️ **Действующее исключение (с 09.09.2026):** панель Neon у пользователя
   > заблокирована, запустить SQL он физически не может. Миграцию `0008` применил
   > агент напрямую через `DATABASE_URL` (права `neondb_owner`) — **с явного разрешения
   > пользователя, спросив отдельно**. Так и продолжать: писать файл миграции, показывать
   > SQL, **спрашивать разрешение** и только потом применять. Молча DDL не запускать.
   > Когда панель починят — вернуться к обычному порядку.
4. **Файлы целиком** при показе изменений — без «...остальное без изменений».
5. **После каждого шага:** `npm run typecheck` + `npm test` → коммит → push → отчёт.
6. **Простота важнее красоты.** Не делаем того, о чём не просили.
7. **Коммиты на русском**, с соавторством агента.

## 9. Где что искать

| Хочешь | Смотри |
|---|---|
| Подробную схему БД с диаграммой | `docs/db-schema.md` |
| Что делаем дальше / чего НЕ делаем | `TODO.md` |
| Краткое описание для людей | `README.md` |
| Исходник схемы (применён в Neon) | `supabase/migrations/0001_init.sql` |
| Реальные ключи подключения | `.env` (локально, не в git) |
