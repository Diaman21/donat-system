-- ============================================================
-- Donat System — миграция 0010
-- Состав заказа: что именно надо купить и сколько закупок это займёт.
--
-- Зачем: заказ может требовать НЕСКОЛЬКО закупок («Орден + Банки» = 2×€30,
-- «Орден + Прем год» = €30 + €105). После первой закупки заказ закрывать
-- НЕЛЬЗЯ — он должен висеть открытым, пока не сделаны все позиции.
--
-- Формат items (jsonb):
--   { "total": 2,
--     "list": [ {"label":"орден","amount":30}, {"label":"банки","amount":30} ] }
--   total — сколько закупок нужно (авторитетно, оператор может поправить руками);
--   list  — разбивка для показа, может отсутствовать, если распознать не удалось.
--
-- NULL = состав ещё не определён (старые заказы и те, что не запускали в работу).
-- ============================================================

alter table order_queue add column items jsonb;
