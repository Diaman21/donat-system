-- ============================================================
-- Donat System — миграция 0005
-- Тип интернета при закупке: 'mobile' (мобильный) | 'wifi' (Wi-Fi).
-- Ещё один аргумент для анализа «зелёного коридора».
-- ============================================================

alter table purchases add column if not exists internet text;
